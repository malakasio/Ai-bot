[phases.setup]
nixPkgs = ["python311", "gcc", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements-railway.txt"]

[start]
cmd = "uvicorn jarvis.api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
