[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.setuptools", "gcc", "ffmpeg"]

[phases.install]
cmds = ["python3 -m pip install -r requirements-railway.txt"]

[start]
cmd = "PYTHONPATH=src uvicorn jarvis.api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
